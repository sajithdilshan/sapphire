$(".ajax-progress").hide()
- if !@ajax_status.nil?
  $(".modal-body > p").text("#{@ajax_status}")
  $("#ajax-status").modal("show");
- else
  $('#feedlistcontainer').html("#{j(render("feeditems/index", cache: true))}")